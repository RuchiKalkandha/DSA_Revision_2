public class prac {

    // find if there is any duplicate
    // brute force approach
    // use nested loops, for every element, check is any other element matches it
    // this will hv time complexity O(n square)

    // more optimised approach
    // sort the array first, then math each element with the next element
    // O(nlogn) time complexity

    // most optimised approach
    // using hash set O(n)


    public static void main(String args[]){

    }
}
